cmake_minimum_required(VERSION 3.25)
project(DesktopPetWinUI3 LANGUAGES CXX)

# 这个文件仅作为源码组织参考。
# WinUI 3 + C++/WinRT 的 XAML 编译、MIDL、MSIX 打包建议使用 Visual Studio 2022 / MSBuild 打开 DesktopPetWinUI3.sln 构建。
message(STATUS "请使用 Visual Studio 2022 打开 DesktopPetWinUI3.sln 构建。")
